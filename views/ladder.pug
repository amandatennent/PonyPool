extends layout

block content
	div.card.title-card Ladder
	div.card
		table
			tr
				th #
				th Punter
				th Winnings
			each element in json
				tr
					td(width="1px") !{element.Place}
					td !{element.Name}
					td(width="1px") !{element.Winnings}
